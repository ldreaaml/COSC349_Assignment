[1mdiff --git a/Vagrantfile b/Vagrantfile[m
[1mindex 4c7500f..52144db 100644[m
[1m--- a/Vagrantfile[m
[1m+++ b/Vagrantfile[m
[36m@@ -2,39 +2,33 @@[m
 # vi: set ft=ruby :[m
 [m
 # All Vagrant configuration is done below. The "2" in Vagrant.configure[m
[31m-# configures the configuration version (we support older styles for[m
[31m-# backwards compatibility). Please don't change it unless you know what[m
[31m-# you're doing.[m
 Vagrant.configure("2") do |config|[m
     [m
     config.vm.box = "ubuntu/xenial64"[m
  [m
[31m-    # first VM - for webserver[m
[31m-    config.vm.define "firstVM" do |firstVM|[m
[31m-        firstVM.vm.hostname = "firstVM"[m
[32m+[m[32m    # first VM for client webserver[m
[32m+[m[32m    config.vm.define "webserver" do |webserver|[m
[32m+[m[32m        webserver.vm.hostname = "webserver"[m
 [m
         # run on port 80 on the guest VM, and map to port 8080 on the host[m
         # user only allow access via local host ip[m
[31m-        firstVM.vm.network "forwarded_port", guest: 80, host: 8888, host_ip: "127.0.0.1"[m
[32m+[m[32m        webserver.vm.network "forwarded_port", guest: 80, host: 8888, host_ip: "127.0.0.1"[m
 [m
[31m-#        ping -c3 192.168.2.20[m
[31m-[m
[31m-        # private network for communications between multiple VMs[m
[31m-        firstVM.vm.network "private_network", ip: "192.168.2.20"[m
[32m+[m[32m        # webserver network for communications between multiple VMs[m
[32m+[m[32m        webserver.vm.network "private_network", ip: "192.168.2.20"[m
 [m
         # synced folder is used to share directory between host machine and guest VMs[m
[31m-        firstVM.vm.synced_folder ".","/vagrant", owner: "vagrant", group: "vagrant", mount_options: ["dmode=775,fmode=777"][m
[32m+[m[32m        webserver.vm.synced_folder ".","/vagrant", owner: "vagrant", group: "vagrant", mount_options: ["dmode=775,fmode=777"][m
         [m
[31m-        firstVM.vm.provision "shell", inline: <<-SHELL[m
[32m+[m[32m        webserver.vm.provision "shell", inline: <<-SHELL[m
       #install apache package[m
      apt-get update[m
       apt-get install -y apache2 php libapache2-mod-php php-mysql[m
             [m
       # Change VM's webserver's configuration to use shared folder.[m
[31m-      # (Look inside test-website.conf for specifics.)[m
[31m-      cp /vagrant/test-website.conf /etc/apache2/sites-available/[m
[32m+[m[32m      cp /vagrant/user-website.conf /etc/apache2/sites-available/[m
       # activate our website configuration ...[m
[31m-      a2ensite test-website[m
[32m+[m[32m      a2ensite user-website.conf[m
       # ... and disable the default website provided with Apache[m
       a2dissite 000-default[m
       # Reload the webserver configuration, to pick up our changes[m
[36m@@ -43,48 +37,44 @@[m [mVagrant.configure("2") do |config|[m
 SHELL[m
 end[m
     [m
[31m-    # second VM - for admin [m
[31m-        # second VM - for adminwebserver[m
[31m-        config.vm.define "secondVM" do |secondVM|[m
[31m-            secondVM.vm.hostname = "secondVM"[m
[32m+[m[32m        # second VM for adminwebserver[m
[32m+[m[32m        config.vm.define "adminserver" do |adminserver|[m
[32m+[m[32m            adminserver.vm.hostname = "adminserver"[m
 [m
             # run on port 80 on the guest VM, and map to port 8081 on the host[m
             # user only allow access via local host ip[m
[31m-            secondVM.vm.network "forwarded_port", guest: 80, host: 8889, host_ip: "127.0.0.1"[m
[31m-[m
[31m-    #        ping -c3 192.168.2.21[m
[32m+[m[32m            adminserver.vm.network "forwarded_port", guest: 80, host: 8889, host_ip: "127.0.0.1"[m
 [m
             # private network for communications between multiple VMs[m
[31m-            secondVM.vm.network "private_network", ip: "192.168.2.21"[m
[32m+[m[32m            adminserver.vm.network "private_network", ip: "192.168.2.21"[m
 [m
             # synced folder is used to share directory between host machine and guest VMs[m
[31m-            secondVM.vm.synced_folder ".","/vagrant", owner: "vagrant", group: "vagrant", mount_options: ["dmode=775,fmode=777"][m
[32m+[m[32m            adminserver.vm.synced_folder ".","/vagrant", owner: "vagrant", group: "vagrant", mount_options: ["dmode=775,fmode=777"][m
             [m
[31m-            secondVM.vm.provision "shell", inline: <<-SHELL[m
[32m+[m[32m            adminserver.vm.provision "shell", inline: <<-SHELL[m
           #install apache package[m
          apt-get update[m
           apt-get install -y apache2 php libapache2-mod-php php-mysql[m
                 [m
           # Change VM's webserver's configuration to use shared folder.[m
[31m-          # (Look inside test-website.conf for specifics.)[m
[31m-          cp /vagrant/test-website1.conf /etc/apache2/sites-available/[m
[32m+[m[32m          cp /vagrant/admin-website.conf /etc/apache2/sites-available/[m
           # activate our website configuration ...[m
[31m-          a2ensite test-website1[m
[32m+[m[32m          a2ensite admin-website.conf[m
           # ... and disable the default website provided with Apache[m
           a2dissite 000-default[m
           # Reload the webserver configuration, to pick up our changes[m
           service apache2 reload[m
 [m
[31m-    SHELL[m
[32m+[m[32mSHELL[m
         [m
     end[m
     [m
[31m-    # third VM- for db[m
[31m-    config.vm.define "thirdVM" do |thirdVM|[m
[31m-        thirdVM.vm.hostname = "third-machine"[m
[31m-        thirdVM.vm.network "private_network", ip: "192.168.2.22"[m
[31m-        thirdVM.vm.synced_folder ".","/vagrant", owner: "vagrant", group: "vagrant", mount_options: ["dmode=775,fmode=777"][m
[31m-thirdVM.vm.provision "shell", inline: <<-SHELL[m
[32m+[m[32m    # third VM for database[m
[32m+[m[32m    config.vm.define "dbserver" do |dbserver|[m
[32m+[m[32m        dbserver.vm.hostname = "third-machine"[m
[32m+[m[32m        dbserver.vm.network "private_network", ip: "192.168.2.22"[m
[32m+[m[32m        dbserver.vm.synced_folder ".","/vagrant", owner: "vagrant", group: "vagrant", mount_options: ["dmode=775,fmode=777"][m
[32m+[m[32mdbserver.vm.provision "shell", inline: <<-SHELL[m
       # Update Ubuntu software packages.[m
       apt-get update[m
       [m
[36m@@ -139,7 +129,7 @@[m [mthirdVM.vm.provision "shell", inline: <<-SHELL[m
       # We then restart the MySQL server to ensure that it picks up[m
       # our configuration changes.[m
       service mysql restart[m
[31m-    SHELL[m
[32m+[m[32mSHELL[m
    [m
  end[m
 [m
[1mdiff --git a/test-website.conf b/test-website.conf[m
[1mdeleted file mode 100644[m
[1mindex 756fb66..0000000[m
[1m--- a/test-website.conf[m
[1m+++ /dev/null[m
[36m@@ -1,34 +0,0 @@[m
[31m-<VirtualHost *:80>[m
[31m-	# The ServerName directive sets the request scheme, hostname and port that[m
[31m-	# the server uses to identify itself. This is used when creating[m
[31m-	# redirection URLs. In the context of virtual hosts, the ServerName[m
[31m-	# specifies what hostname must appear in the request's Host: header to[m
[31m-	# match this virtual host. For the default virtual host (this file) this[m
[31m-	# value is not decisive as it is used as a last resort host regardless.[m
[31m-	# However, you must set it for any further virtual host explicitly.[m
[31m-	#ServerName www.example.com[m
[31m-[m
[31m-	ServerAdmin webmaster@localhost[m
[31m-	DocumentRoot /vagrant/www[m
[31m-	<Directory /vagrant/www/>[m
[31m-		Require all granted[m
[31m-	</Directory>[m
[31m-[m
[31m-	# Available loglevels: trace8, ..., trace1, debug, info, notice, warn,[m
[31m-	# error, crit, alert, emerg.[m
[31m-	# It is also possible to configure the loglevel for particular[m
[31m-	# modules, e.g.[m
[31m-	#LogLevel info ssl:warn[m
[31m-[m
[31m-	ErrorLog ${APACHE_LOG_DIR}/error.log[m
[31m-	CustomLog ${APACHE_LOG_DIR}/access.log combined[m
[31m-[m
[31m-	# For most configuration files from conf-available/, which are[m
[31m-	# enabled or disabled at a global level, it is possible to[m
[31m-	# include a line for only one particular virtual host. For example the[m
[31m-	# following line enables the CGI configuration for this host only[m
[31m-	# after it has been globally disabled with "a2disconf".[m
[31m-	#Include conf-available/serve-cgi-bin.conf[m
[31m-</VirtualHost>[m
[31m-[m
[31m-# vim: syntax=apache ts=4 sw=4 sts=4 sr noet[m
[1mdiff --git a/test-website1.conf b/test-website1.conf[m
[1mdeleted file mode 100644[m
[1mindex 2d61e1c..0000000[m
[1m--- a/test-website1.conf[m
[1m+++ /dev/null[m
[36m@@ -1,34 +0,0 @@[m
[31m-<VirtualHost *:80>[m
[31m-	# The ServerName directive sets the request scheme, hostname and port that[m
[31m-	# the server uses to identify itself. This is used when creating[m
[31m-	# redirection URLs. In the context of virtual hosts, the ServerName[m
[31m-	# specifies what hostname must appear in the request's Host: header to[m
[31m-	# match this virtual host. For the default virtual host (this file) this[m
[31m-	# value is not decisive as it is used as a last resort host regardless.[m
[31m-	# However, you must set it for any further virtual host explicitly.[m
[31m-	#ServerName www.example.com[m
[31m-[m
[31m-	ServerAdmin webmaster@localhost[m
[31m-	DocumentRoot /vagrant/wwwAdmin[m
[31m-	<Directory /vagrant/wwwAdmin/>[m
[31m-		Require all granted[m
[31m-	</Directory>[m
[31m-[m
[31m-	# Available loglevels: trace8, ..., trace1, debug, info, notice, warn,[m
[31m-	# error, crit, alert, emerg.[m
[31m-	# It is also possible to configure the loglevel for particular[m
[31m-	# modules, e.g.[m
[31m-	#LogLevel info ssl:warn[m
[31m-[m
[31m-	ErrorLog ${APACHE_LOG_DIR}/error.log[m
[31m-	CustomLog ${APACHE_LOG_DIR}/access.log combined[m
[31m-[m
[31m-	# For most configuration files from conf-available/, which are[m
[31m-	# enabled or disabled at a global level, it is possible to[m
[31m-	# include a line for only one particular virtual host. For example the[m
[31m-	# following line enables the CGI configuration for this host only[m
[31m-	# after it has been globally disabled with "a2disconf".[m
[31m-	#Include conf-available/serve-cgi-bin.conf[m
[31m-</VirtualHost>[m
[31m-[m
[31m-# vim: syntax=apache ts=4 sw=4 sts=4 sr noet[m
